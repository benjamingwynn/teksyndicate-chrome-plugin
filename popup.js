/*
var background = chrome.extension.getBackgroundPage();

// get the url
chrome.tabs.query({active: true, currentWindow: true}, function(tabs) {
	// only one tab is returned
	var tab = tabs[0];

	background.console.log("tests said that " + tab.url + " is url");
});
*/
